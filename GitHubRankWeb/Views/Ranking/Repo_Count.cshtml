
@{
    ViewData["Title"] = "Repository Count Ranking";
}

@model List<RankItem>

<div>
    <h1>GitHub Repository Count Ranking</h1>
    <div>
        <div class="row">
            <a class="page-link" href="/ranking/repo_count?type=user">User</a>
            <a class="page-link" href="/ranking/repo_count?type=organization">Organization</a>
        </div>
    </div>
    <div>
        <h2>Most public repository @ViewData["type"]</h2>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Rank</th>
                    <th>Name</th>
                    <th>Repository</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < 100 && i < Model.Count; i++)
                {
                    var rankItem = Model[i];
                    <tr>
                        <td>@Html.DisplayFor(e => rankItem.Rank)</td>
                        <td>
                            <a href="@rankItem.Reference" style="text-decoration: none">
                                <img class="avatar_image" src="@rankItem.AvatarUrl" />
                            </a>
                            <a href="@rankItem.Reference">@rankItem.Name</a>
                        </td>
                        <td>@Html.DisplayFor(e => rankItem.Score)</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
